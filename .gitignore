# dependencies (never commit — huge, and rebuilt with `npm install`)
node_modules/

# build output (rebuilt with the build scripts)
dist/
dist-ssr/
*.local

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor / OS junk
.DS_Store
Thumbs.db
*.tmp
*.tmp.*

# personal Claude Code settings (machine-specific)
.claude/settings.local.json
